package ar.edu.unlam.mobile.scaffolding.domain.model

/**
 * Estos son los dos modos de búsqueda de mascotas.
 * ROUTE: Muestra una polilínea azul que sigue las calles.
 * RADAR: Muestra una flecha roja que apunta directamente a la mascota (línea recta).
 */
enum class SearchMode {
    ROUTE,
    RADAR
}
